class NodeArray extends Array {
    // constructor(args) {
    // 	// code
    // }
    contains(node) {
        return this.filter(el => this.isIdentical(el === true));
        // methods
    }
}

module.exports = NodeArray;